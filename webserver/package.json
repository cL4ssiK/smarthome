{
  "name": "project-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "docker compose up -d",
    "devstart": "docker compose up -d && docker logs -f serverContainer",
    "stop": "docker compose down",
    "build": "docker compose build",
    "build:server": "docker compose build server"
  }
}